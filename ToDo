To Do:

Embed Tableau into index page

Flask app — converts SQLite to JSON
	
	endpoints: index, density, facts, reveal

JS app — reads JSON and converts to live graphs
	bubble chart by state, bar chart by region
